// -----------------------------------------------------------------------------------------------------------------------------
// Data una stringa, conta quante vocali contiene.

// let wordexample = "goodbye";
// let numerOfVocals = 0;
// let vocals = ["a", "e", "i", "o", "u"];

// for (let lettera of wordexample) {
//   if (vocals.includes(lettera)) {
//     numerOfVocals++;
//   }
// }

// console.log(numerOfVocals);
// -----------------------------------------------------------------------------------------------------------------------------

// -----------------------------------------------------------------------------------------------------------------------------
// Calcola il fattoriale di un numero con una funzione ricorsiva.

// function factorialCalculation(n) {
//   accumulator = 1;
//   if (n == 1) {
//     return n;
//   } else {
//     accumulator = n * factorialCalculation(n - 1);
//     return accumulator;
//   }
// }

// console.log(factorialCalculation(5));
// -----------------------------------------------------------------------------------------------------------------------------

// -----------------------------------------------------------------------------------------------------------------------------
// Scrivi una funzione che genera una password casuale di N caratteri alfanumerici.

// function passwordGenerator(length) {
//   const characters =
//     "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ123456789";
//   let password = "";
//   for (i = 0; i < length; i++) {
//     let randomIndex = Math.floor(Math.random() * characters.length);
//     password += characters[randomIndex];
//   }
//   return password;
// }

// console.log(passwordGenerator(20));
// -----------------------------------------------------------------------------------------------------------------------------

// -----------------------------------------------------------------------------------------------------------------------------
// Scrivi una funzione che verifica se una parola è palindroma (es. “anna”, “otto”).

// function checkPalindromicWord(word) {
//   word = word.split().reverse();
// }

// split , reverse e funzione che riattacca
// -----------------------------------------------------------------------------------------------------------------------------

// -----------------------------------------------------------------------------------------------------------------------------
// Crea un array di oggetti utente { nome, età }. Scrivi funzioni per:
//     Aggiungere un utente
//     Trovare tutti gli utenti maggiorenni
//     Calcolare l’età media
// -----------------------------------------------------------------------------------------------------------------------------

// -----------------------------------------------------------------------------------------------------------------------------
// Scrivi un programma in JavaScript che:
//     Generi 1000 numeri casuali interi compresi tra 0 e 9, utilizzando.
//     Salvi questi numeri in un array.
//     Calcoli quante volte ciascun numero (da 0 a 9) compare nell’array.
//     Restituisca un array di 10 elementi, dove ciascun elemento rappresenta il conteggio delle occorrenze del numero corrispondente all’indice.
// -----------------------------------------------------------------------------------------------------------------------------
